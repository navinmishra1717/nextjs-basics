export interface ProductData {
    id: string;
    name: string;
    price: number;
    description: string;
}

export interface UserData {
    id: string;
    name: string;
    company: string;
    age: number;
}